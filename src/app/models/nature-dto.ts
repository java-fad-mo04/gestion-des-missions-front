export class NatureDto {

  // tslint:disable-next-line: max-line-length
  constructor(public id: number, public code: string, public isFacturee: string, public hasPrime: string, public pourcentagePrime: number, public depassPlafond: string, public debutValidite: string, public finValidite: string, public tjm: number) {
  }
}
